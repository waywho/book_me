<div data-controller="modal-component">
	<input type="checkbox" id="my-modal-6" class="modal-toggle" />
	<div class="modal modal-bottom sm:modal-middle <%= 'modal-open' if @auto %>" data-modal-component-target="modal">
		<div class="modal-box relative">
			 <button for="my-modal-6" class="btn btn-sm btn-outline btn-circle absolute border-0 right-2 top-2" data-action="modal-component#close">✕</button>
			<%= content %>

			<% if buttons? %>
				<div class="modal-action">
					<% buttons.each do |button| %>
						<%= button %>
					<% end %>
				</div>
			<% end %>
		</div>
	</div>
</div>
