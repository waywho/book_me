<h1>Calendars#index</h1>
<p>We found these calendars in your google</p>


<% @calendars.each do |calendar| %>
	<%= button_to "Connect to #{calendar.id}", google_calendars_path(id: calendar.id) %>)</li>
<% end %>
